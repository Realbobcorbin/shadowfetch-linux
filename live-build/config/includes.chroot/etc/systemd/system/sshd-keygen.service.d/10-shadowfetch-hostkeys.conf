[Unit]
# Calamares installs from a live image with an existing machine-id, so
# systemd's ConditionFirstBoot is false even on the first installed boot.
ConditionFirstBoot=
ConditionPathExists=!/etc/ssh/ssh_host_ed25519_key
