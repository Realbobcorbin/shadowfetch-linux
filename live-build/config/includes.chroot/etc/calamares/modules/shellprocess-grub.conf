# Shadowfetch firmware-native bootloader install. The script resolves mapper and
# partition stacks to the physical disk, installs the active firmware target,
# and logs to /var/log/sf-install-grub.log on the installed system.
dontChroot: false
timeout: 700
script:
  - "sh /usr/local/sbin/sf-install-grub"
